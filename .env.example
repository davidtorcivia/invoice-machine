# Application URL (used for PDF links and MCP config display)
# Set this to your public URL in production
APP_BASE_URL=http://localhost:8080

# Database URL (SQLite)
DATABASE_URL=sqlite+aiosqlite:///./data/invoicely.db

# Data directory for database, PDFs, logos, and backups
DATA_DIR=./data

# Trash retention (days before auto-purge)
TRASH_RETENTION_DAYS=90

# CORS origins (comma-separated, for development)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
