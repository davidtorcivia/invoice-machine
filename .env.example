# Application URL (for PDF links in MCP responses)
# Local dev: http://localhost:8080
# LAN access: http://192.168.1.x:8080
# Cloudflare Tunnel: https://invoices.yourdomain.com
APP_BASE_URL=http://localhost:8080

# Database URL (SQLite default)
DATABASE_URL=sqlite+aiosqlite:///./data/invoicely.db

# Trash retention days (auto-purge after this many days)
TRASH_RETENTION_DAYS=90

# Security settings
# CORS origins (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Optional API key for protecting the API (leave empty to disable)
# Set this in production to protect your API
API_KEY=

# MCP API key for remote access via SSE transport
# Required when accessing MCP from another machine (LAN or internet)
# Generate with: openssl rand -hex 32
MCP_API_KEY=
